# 🤖 Мультиагентная система разработки

## Общая архитектура
Система состоит из 9 специализированных агентов, координируемых оркестратором. Все агенты работают по принципу "сверху-вниз" с акцентом на early end-to-end проверки.

## 📋 Список агентов

| Роль | Файл | Ключевая ответственность |
|------|------|--------------------------|
| **Оркестратор** | [`01_orchestrator.md`](agents/01_orchestrator.md) | Координация процесса, управление циклами review, остановка при блокирующих вопросах |
| **Аналитик** | [`agents/02_analyst.md`](02_analyst.md) | Создание ТЗ с юзер-кейсами, сценариями и критериями приёмки |
| **Ревьюер ТЗ** | [`agents/03_tz_reviewer.md`](03_tz_reviewer_prompt.md) | Проверка полноты и качества технического задания |
| **Архитектор** | `agents/04_architect.md` | Проектирование функциональной и системной архитектуры |
| **Ревьюер архитектуры** | `agents/05_architecture_reviewer.md` | Проверка соответствия архитектуры ТЗ и существующему проекту |
| **Планировщик** | `agents/06_planner.md` | Создание низкоуровневого плана с детальными задачами |
| **Ревьюер плана** | `agents/07_plan_reviewer.md` | Проверка покрытия юзер-кейсов и наличия описаний задач |
| **Разработчик** | `agents/08_developer.md` | Реализация кода и тестов строго по описанию задач |
| **Ревьюер кода** | `agents/09_code_reviewer.md` | Проверка качества кода и тестов |

## 🔁 Процесс работы
1. **Анализ**: Аналитик → Ревьюер ТЗ (макс. 2 итерации)
2. **Проектирование**: Архитектор → Ревьюер архитектуры (макс. 2 итерации)
3. **Планирование**: Планировщик → Ревьюер плана (макс. 2 итерации)
4. **Реализация**: Разработчик → Ревьюер кода для каждой задачи (макс. 2 итерации)

## ⚠️ Ключевые правила
- **Не выходить за рамки своей роли**
- **Не рефакторить без явного указания**
- **Задавать вопросы при неясностях** (оркестратор остановит процесс)
- **Документировать каждое изменение**

> **Важно:** Все агенты должны строго следовать спецификациям в соответствующих файлах. При конфликте версий — приоритет у последней версии файла.


Отлично — тогда тебе нужна **классификационная модель**, которая **не прогнозирует цену**, а **решает задачу: BUY / SELL / HOLD** только по техническим индикаторам.

Ниже — точный план, архитектура и пример того, что именно нужно делать.

---

# ✅ **План для модели, выдающей BUY / SELL / HOLD только по индикаторам**

---

## **1. Сбор данных (только OHLCV)**

Это всё, что нужно для расчёта тех. индикаторов.

**Для каждого тикера IMOEX:**

* Date
* Open
* High
* Low
* Close
* Volume

---

## **2. Генерация технических индикаторов (основа модели)**

Чтобы модель давала сигналы, нужно много производных фич:

### 🔹 Трендовые

* SMA(5), SMA(10), SMA(20), SMA(50)
* EMA(10), EMA(20), EMA(50)
* MACD, MACD_signal, MACD_hist

### 🔹 Осцилляторы

* RSI(14)
* Stochastic (%K, %D)
* CCI
* Williams %R

### 🔹 Волатильность

* ATR(14)
* Bollinger Bands (upper, middle, lower)
* Price-to-BB (позиция цены относительно полос)

### 🔹 Объёмные

* Volume ROC
* Volume SMA ratio

### 🔹 Свечные паттерны (желательно)

* Doji
* Hammer
* Engulfing

Используй библиотеку `ta`:

```python
import ta

df["rsi"] = ta.momentum.RSIIndicator(df["Close"]).rsi()
df["macd"] = ta.trend.MACD(df["Close"]).macd()
df["ema20"] = ta.trend.EMAIndicator(df["Close"], 20).ema_indicator()
df["atr"] = ta.volatility.AverageTrueRange(df["High"], df["Low"], df["Close"]).average_true_range()
```

---

# **3. Создание целевой переменной (TARGET) для BUY / SELL / HOLD**

Ты НЕ предсказываешь цену.

Ты обучаешь модель по правилу:

### **Вариант 1 (самый логичный для индикаторов):**

Сигнал определяется будущим движением цены на фиксированное окно.

Например:

```
Если цена через 5 дней выше на +1% → BUY  
Если ниже на −1% → SELL  
Иначе → HOLD
```

Формула:

```python
df["future_return"] = df["Close"].shift(-5) / df["Close"] - 1

df["signal"] = 0
df.loc[df["future_return"] > 0.01, "signal"] = 1   # BUY
df.loc[df["future_return"] < -0.01, "signal"] = -1 # SELL
```

Почему так?
Потому что модель должна знать, какой индикаторный паттерн приводил к росту/падению.

Модель не знает будущую цену, **она учится распознавать индикаторные комбинации приводящие к росту или падению**.

---

# **4. Обучение классификатора**

Самые эффективные модели:

### 🔹 CatBoostClassifier (рекомендуется)

* отлично работает на табличных данных
* не требует нормализации
* устойчив к шуму

### 🔹 XGBoost / LightGBM

### 🔹 Для продвинутых — нейросети MLP (но хуже деревьев)

Пример на CatBoost:

```python
from catboost import CatBoostClassifier

X = df[indicators_columns]
y = df["signal"]

model = CatBoostClassifier(
    iterations=400,
    depth=8,
    learning_rate=0.05,
    loss_function="MultiClass"
)

model.fit(X, y, verbose=False)
```

---

# **5. Валидация (очень важно)**

Ты работаешь с временем → нельзя перемешивать данные.

Используй:

* Train: 2014–2021
* Valid: 2021–2023
* Test: 2023–2025

Метрики:

* Accuracy
* Precision BUY / SELL
* Recall BUY / SELL
* Profit factor
* Backtest equity curve

---

# **6. Backtesting торговых сигналов**

После получения сигналов:

* BUY → покупка
* SELL → короткая продажа (или выход в кэш)
* HOLD → ничего не делаем

Симуляция:

* stop-loss (2–5%)
* take-profit (3–6%)
* комиссия MOEX

Можно использовать `backtrader` или свой код.

---

# **7. Деплой модели**

Делаешь:

* Telegram-бота, отправляющего сигналы
* Web-API для использования в любом приложении
* Скрипт на cron, который каждый день:

  1. Загружает последние данные
  2. Пересчитывает индикаторы
  3. Генерирует сигнал
  4. Отправляет результат

---

# 📌 Итог: Реальная схема работы модели

1. Берём цену за последние 30–60 дней.
2. Превращаем её в набор технических индикаторов.
3. Модель классифицирует:

   * BUY → индикаторы указывают на будущий рост
   * SELL → индикаторы указывают на падение
   * HOLD → сигнала нет
4. Никаких прогнозов цены не используется.

---

# 📦 Готов предоставить:

✅ Полный Python-код (загрузка данных → индикаторы → модель → backtest)
✅ Полный Jupyter Notebook
✅ Готовую CatBoost-модель под твои тикеры
✅ Архитектуру телеграм-бота для сигналов

Что хочешь первым?
